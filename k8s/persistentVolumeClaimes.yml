kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mysql-pvc
  namespace: bankapp-ns
spec:
  accessModes:
    -ReadWriteOnce
  storageClass: manual
  resource:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app: mysql